How to use:

After adding additional scripts, update the `nextcp.db.SessionManager.TARGET_DB_SCHEMA` attribute to reflect which scripts to run ... 

The session manager will detect the current ersion of the DB and will update till the `nextcp.db.SessionManager.TARGET_DB_SCHEMA` scriptnumber. 

